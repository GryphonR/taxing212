/**
 * @file main.js
 * @brief Browser entry point for the taxing212 Vue application.
 */
import Vue from 'vue';
import { mountApp } from './vue-app.js';

Vue.config.productionTip = false;
mountApp(Vue);
