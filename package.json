{
  "name": "taxing212",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "vite",
    "build": "node scripts/build-app.mjs",
    "build:app": "node scripts/build-app.mjs",
    "preview": "npm run build && npx --yes serve -l 8000 ."
  },
  "dependencies": {
    "decimal.js": "^10.4.3",
    "luxon": "^2.5.2",
    "papaparse": "^5.5.3",
    "vue": "^2.7.16"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
